apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-api-chart.fullname" . }}
  labels:
    {{- include "image-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-api-chart.selectorLabels" . | nindent 4 }}
