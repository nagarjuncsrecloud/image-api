apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "image-api.fullname" . }}-test-connection"
  labels:
    {{- include "image-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "image-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
